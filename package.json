{
  "name": "scentic-agpl-services",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0",
  "description": "AGPL-licensed integration stack for Scentic (Kimai + OpenSign gateway)",
  "scripts": {
    "install:all": "pnpm install",
    "typecheck": "pnpm --filter gateway typecheck",
    "lint": "pnpm --filter gateway lint",
    "test": "pnpm --filter gateway test",
    "build": "pnpm --filter gateway build",
    "dev": "pnpm --filter gateway dev"
  },
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3"]
  }
}
